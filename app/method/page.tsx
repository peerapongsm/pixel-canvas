import Link from "next/link";

export const metadata = {
  title: "วิธีการทำงาน — Pixel Canvas สองคน",
};

export default function MethodPage() {
  return (
    <>
      <header className="app-header">
        <h1>วิธีการทำงาน</h1>
        <div className="nav-buttons">
          <Link href="/" className="btn btn-outline">
            กลับหน้าหลัก
          </Link>
        </div>
      </header>
      <main>
        <div className="method-content">
          <h2>ไม่มี server กลางเลย</h2>
          <p>
            แอปนี้ไม่มี backend, ไม่มีฐานข้อมูล, ไม่มี server เก็บภาพวาดของคุณที่ไหนทั้งนั้น การเชื่อมต่อเป็นแบบ{" "}
            <strong>WebRTC DataChannel</strong> ตรงระหว่างเบราว์เซอร์สองเครื่อง — เมื่อวาด pixel ข้อมูลจะวิ่งจากเครื่องคุณ
            ไปเครื่องเพื่อนโดยตรง ไม่ผ่านเซิร์ฟเวอร์ของใครทั้งสิ้น (ไม่ใช่ของเรา ไม่ใช่ของใครอื่น)
          </p>

          <h2>แล้วสองเครื่องรู้จักกันได้ยังไงถ้าไม่มี server?</h2>
          <p>
            การต่อ WebRTC ต้องมีขั้นตอน &quot;signaling&quot; แลกข้อมูลเทคนิค (SDP) กันก่อนครั้งเดียว ปกติแอปอื่นจะใช้
            server กลางส่งข้อมูลนี้ให้อัตโนมัติ แต่แอปนี้ตัด server กลางออกทั้งหมด แล้วให้{" "}
            <strong>คุณเป็นคนส่งข้อมูลนั้นเอง</strong> ผ่านช่องทางไหนก็ได้ที่คุณสะดวก (LINE, ข้อความ, อะไรก็ได้):
          </p>
          <ul>
            <li>คนเปิดห้องกด &quot;เปิดห้อง&quot; → ได้ &quot;ลิงก์ชวน&quot; → ส่งให้เพื่อน (หรือให้สแกน QR)</li>
            <li>เพื่อนกดลิงก์ → เข้าห้องอัตโนมัติ ได้ &quot;โค้ดตอบ&quot; สั้น ๆ → ส่งกลับมา</li>
            <li>คนเปิดห้องวางโค้ดตอบ → ต่อติด</li>
          </ul>
          <p>
            ลิงก์ชวนแนบข้อมูล SDP ไว้ใน URL fragment (ส่วนหลัง <code>#</code>) ซึ่งเบราว์เซอร์{" "}
            <strong>ไม่ส่งไปกับ HTTP request</strong> — ต่อให้เปิดผ่านลิงก์ ข้อมูลเชื่อมต่อก็ยังไม่ผ่าน server ใด ๆ
            อยู่ดี ตัวโค้ดสั้นลงมากเพราะเราแกะเฉพาะฟิลด์ที่เปลี่ยนจริงจาก SDP (ice-ufrag/pwd, DTLS fingerprint,
            candidate) มาแพ็กเป็น binary แล้วประกอบ SDP กลับจาก template ฝั่งรับ (<code>lib/shortCode.ts</code>)
            หลังจากขั้นตอนนี้เสร็จ ทั้งสองเครื่องคุยกันตรงตลอดเซสชัน ไม่ต้องแลกโค้ดซ้ำอีก
          </p>

          <h2>ข้อจำกัดของ NAT (ต่อไม่ติดบ้างเป็นเรื่องปกติ)</h2>
          <p>
            แอปนี้ใช้ STUN server สาธารณะของ Google (<code>stun.l.google.com</code>) ช่วยให้สองเครื่องรู้ที่อยู่
            สาธารณะของกันและกัน แต่ <strong>ไม่มี TURN server</strong> (ตัวรีเลย์สำรองเวลาต่อตรงไม่ได้) เพราะการมี TURN
            หมายถึงต้องมี server กลางรับส่งข้อมูลจริง ๆ ซึ่งขัดกับหลักการ &quot;ไม่มี server&quot; ของโปรเจกต์นี้ ผลคือ:
            เครือข่ายบางคู่ (โดยเฉพาะ NAT แบบ symmetric ที่พบใน Wi-Fi องค์กร/มหาวิทยาลัยบางที่ หรือเน็ตมือถือบางค่าย)
            อาจต่อกันตรงไม่ได้เลย ถ้าเจอแบบนี้ ลองสลับไปใช้เน็ตอื่น (เช่นจาก Wi-Fi เป็นเน็ตมือถือ) มักช่วยได้ —
            งานวาดที่มีอยู่จะไม่หายไป และเล่นคนเดียวยังใช้ได้ตามปกติเสมอ
          </p>

          <h2>ความเป็นส่วนตัว</h2>
          <p>
            เพราะภาพวิ่งตรงระหว่างสองเครื่องเท่านั้น จึงไม่มีใครนอกจากสองคนที่เชื่อมต่อกันเห็นภาพวาดนี้ได้ — ไม่มีการ
            อัปโหลดขึ้น server ไหน ไม่มีการเก็บ log ฝั่งเรา สิ่งที่เก็บมีแค่ใน <code>localStorage</code> ของเบราว์เซอร์
            คุณเอง (สำหรับกันงานหายตอนรีเฟรช) เท่านั้น
          </p>

          <h2>ทำไม TDD แค่บางไฟล์</h2>
          <p>
            <code>lib/grid.ts</code> (LWW conflict resolution), <code>lib/proto.ts</code> (encode/decode ข้อความ),
            <code>lib/shortCode.ts</code> (แพ็ก SDP เป็นโค้ดสั้น + validate) และ <code>lib/undo.ts</code> (ย้อนกลับ
            แบบไม่ทับงานเพื่อน) คือตรรกะที่ผิดพลาดแล้วทำข้อมูลเพี้ยนหรือเชื่อมต่อไม่ได้เลย เลยเทสต์แน่นด้วย TDD
            ส่วนการเชื่อมต่อ WebRTC จริง (<code>lib/rtc.ts</code>) และ UI (zoom/pan/canvas) เป็น &quot;ของจริง&quot;
            ที่ต้องอาศัย browser API (<code>RTCPeerConnection</code>) ซึ่งไม่มีใน environment ทดสอบ (jsdom) —
            การต่อจริงจึงยืนยันด้วยการทดสอบ 2 เบราว์เซอร์ (ดู README)
          </p>
        </div>
      </main>
      <footer className="app-footer">
        <p>Pixel Canvas สองคน · ไม่มี server กลาง ภาพวิ่งตรงระหว่างสองเครื่องเท่านั้น</p>
      </footer>
    </>
  );
}
